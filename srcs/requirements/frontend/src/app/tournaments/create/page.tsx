import { ComingSoon } from "@/components/ui/ComingSoon";

export default function CreateTournamentPage() {
    return (
        <ComingSoon
            title="create tournament"
            description="organize your own events with customized rules, prizes, and participants. management tools are under development."
        />
    );
}
