import { ComingSoon } from "@/components/ui/ComingSoon";

export default function TournamentsPage() {
    return (
        <ComingSoon
            title="tournaments"
            description="browse active tournaments, check brackets, and join the competition. the arena is being prepared."
        />
    );
}
