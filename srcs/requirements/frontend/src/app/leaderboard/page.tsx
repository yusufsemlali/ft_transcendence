import { ComingSoon } from "@/components/ui/ComingSoon";

export default function LeaderboardPage() {
    return (
        <ComingSoon
            title="leaderboard"
            description="see who is dominating the rankings across all games and regions. track your progress toward the top."
        />
    );
}
